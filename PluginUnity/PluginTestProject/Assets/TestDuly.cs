using UnityEngine;

public class TestDuly : DulyBehaviour
{
    // Use this for initialization
    private void Start()
    {
        Debug.Log("Executing !");
        Execute();
    }

    // Update is called once per frame
    private void Update()
    {
    }
}